import { Request, Response, NextFunction} from 'express';
import log from '../config/log';
import mongoose from 'mongoose';

const namespace = 'SCHEDULE-CONTROLLER'

const getEvents = (req: Request, res: Response, next: NextFunction) => {

}
const createEvent = (req: Request, res: Response, next: NextFunction) => {
    
}
const deleteEvent = (req: Request, res: Response, next: NextFunction) => {
    
}
const getSingleEvent = (req: Request, res: Response, next: NextFunction) => {
    
}